# View Commit History

Show recent commit history with details.

## Instructions

1. Run `git log --oneline -10` to show the last 10 commits
2. Ask the user if they want to see details for any specific commit
3. If they specify a commit, run `git show <commit-hash>` to show the full diff
4. Present the information in a readable format
